<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/main_logo_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/round_logo_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.YourAble"
        tools:targetApi="31">

        <service
            android:name=".AudioRecordingService"
            android:foregroundServiceType="microphone" />
        <receiver android:name=".NotificationAction" />

        <activity
            android:name=".ConversationPage"
            android:exported="false" />

        <activity
            android:name=".MainPageBlind"
            android:exported="false" />

        <activity
            android:name=".MainPageDeaf"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PersonalisationPage"
            android:exported="false"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
</manifest>
